<p id="dropdown-title">Add to playlist</p>
<% playlists.each do |playlist| %>
  <%= link_to playlist.name, playlist_tracks_path(playlist, track.id), data: {turbo_method: :post}, class: "dropdown-item" %>
<% end %>
